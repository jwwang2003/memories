import * as React from "preact/compat";
import * as ReactDOM from "preact/compat";
import './index.css';

import App from "./App";

ReactDOM.render(
  <React.StrictMode>
    <App />
  </React.StrictMode>,
  document.getElementById("root")
);